CSP: 
	index:php > TERMINADO
	resto > quitar el 'unsafe-inline' separando los styles y scrips
	
SQL injection:
	modifyEntry.js > quitar la sentencia SQL y hacer en la API update_entry.php
	index.js > quitar la sentencia SQL y hacer en la API update_entry.php
